# erp-procurement

`procurement` microservice extracted from [buildwithtalia/enterprise-resource-planning](https://github.com/buildwithtalia/enterprise-resource-planning).

## Run locally
```
docker build -t erp-procurement .
docker run --rm -p $PORT:$PORT erp-procurement
```
